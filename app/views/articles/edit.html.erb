
<!-- form_for(@VARIABLE_INICIALIZADA_EN_EL_CONTROLADOR) 
nos permite crear el formulario para la variable inicializada desde el modelo
-->
<!-- En este renderizado de la vista parcial se le pasan variables locales, en este
caso el nombre de la accion del formulario que se va a ver reflejada en el titulo
del mismo.
 -->
<%= render "form", accion_formulario: "Editar" %>
